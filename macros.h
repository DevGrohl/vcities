#ifndef MACROS_H
#define MACROS_H


//#define round(X) (int)(floor(X)+0.5)
#define MAYOR(a,b) (a>b?a:b)
#define MENOR(a,b) (a<b?a:b)

#define gra_a_rad(X) (X)*M_PI/180.0
#define rad_a_gra(X) double(X)*180.0/M_PI

#endif
